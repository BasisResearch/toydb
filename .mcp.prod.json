{
  "_comment": "SPDX-License-Identifier: MIT. Verus telemetry (Component 2). PINNED alias: same per-session stdio launcher as the default .mcp.json, kept as a separate file so older instructions ('switch to .mcp.prod.json') still work. VERUS_MCP_REF in .claude/bin/verus-mcp pins the installed build. See .claude/hooks/README.md.",
  "mcpServers": {
    "verus": {
      "type": "stdio",
      "command": ".claude/bin/verus-mcp",
      "args": [
        "stdio"
      ],
      "timeout": 600000
    }
  }
}
